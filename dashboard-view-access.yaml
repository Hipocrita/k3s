apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: view-all
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin   # Otorga visibilidad completa, asegúrate de que sea seguro
subjects:
- kind: ServiceAccount
  name: isadmin         # Reemplaza con el nombre correcto si es diferente
  namespace: kubernetes-dashboard
